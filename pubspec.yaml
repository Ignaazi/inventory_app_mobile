name: inventory_app_mobile
description: "A new Flutter project for SIIX Inventory Mobile System."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 📊 GRAPHIC DEPENDENCIES (Versi Stabil Pendukung SDK Lu)
  fl_chart: ^0.66.0  

  # 📷 HARDWARE & CAMERA DEPENDENCIES (Pengobat Error scanner.dart)
  mobile_scanner: ^3.5.7  # 🟢 Kunci utama buat ngilangin 11 error di scanner.dart!
  
  # 🔍 Kebutuhan icon tambahan
  cupertino_icons: ^1.0.6
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# 🎨 KONFIGURASI ASET & FONTS
flutter:
  uses-material-design: true